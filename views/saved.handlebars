<div class="container-fluid nav"></div>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <ul class="nav">
    <li class="nav-item scrape">
    <a class="nav-link active scrape" href="/">Home</a>
    </li>
    </ul>
    </nav>
    


<div class="articles">
    {{#each articles}}
    {{#if saved}}
    <div class="article">
        {{!-- <img src="{{image}}" style="height:100px;"> --}}
        <a href="{{link}}">
            <h2>{{title}}</h2>
        </a>
        {{!-- <p>{{summary}}</p> --}}
        <button class="btn btn-primary unsave" data-id="{{_id}}">UNSAVE</button>
        <button class="btn btn-primary addNote" data-id="{{_id}}">NOTE</button>
        <br>
    </div>
    {{/if}}
    {{/each}}
</div>

{{!-- modal for displaying and creating notes --}}
<div id="noteModal" class="modal fade" role="dialog">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title text-center">Add a note</h4>
            </div>
            <div id="notes" class="modal-body"></div>
        </div>
    </div>
</div>
<div class="container-fluid nav" id="top"></div>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <ul class="nav">
        <li class="nav-item scrape">
            <a class="nav-link active scrape" id="tab" href="#">scrape</a>
        </li>
        <li class="nav-item topo">
            <a class="nav-link" id="this" href="/saved">save</a>
        </li>
    </ul>
</nav>



<div class="container" id=topo>
    <h1><span style="color: red">N<span style="color:black">P</span><span style="color: blue">R</span><span style="color: rgb(245, 237, 237)">
                Animals</span></h1>
                <br>
    <div class="articles">
        <div class="load"></div>
        {{#each_upto articles 8}}
        {{#unless saved}}
        <div class="article">
            <a href="{{link}}">
                <h2>{{title}}</h2>
            </a>
            <p>{{summary}}</p>
            <button class="btn btn-primary save" data-id="{{_id}}">SAVE</button>
            <br>
        </div>
        {{/unless}}
        {{/each_upto}}
    </div>
    <div>